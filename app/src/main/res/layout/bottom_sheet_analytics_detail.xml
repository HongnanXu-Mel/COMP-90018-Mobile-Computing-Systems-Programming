<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bottom_sheet_background">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/tvAnalyticsTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Analytics Detail"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/profile_text_primary"
            android:fontFamily="sans-serif-medium" />

        <ImageView
            android:id="@+id/btnClose"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_close"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:padding="6dp"
            app:tint="@color/profile_text_secondary" />

    </LinearLayout>

    <!-- Content -->
    <TextView
        android:id="@+id/tvAnalyticsMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="This shows detailed information about this metric."
        android:textSize="16sp"
        android:textColor="@color/profile_text_primary"
        android:fontFamily="sans-serif"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="16dp" />

    <!-- Value Display -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/analytics_value_background"
        android:padding="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Current Value:"
            android:textSize="14sp"
            android:textColor="@color/profile_text_secondary"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/tvAnalyticsValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/logo_primary"
            android:fontFamily="sans-serif-medium" />

    </LinearLayout>

</LinearLayout>
